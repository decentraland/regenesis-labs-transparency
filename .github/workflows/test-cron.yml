name: Test Cron

on:
  schedule:
    - cron: '*/5 * * * *'  # Every 5 minutes for testing

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Log timestamp
        run: |
          echo "Cron test running at $(date -u)"
          echo "GitHub event: ${{ github.event_name }}"
